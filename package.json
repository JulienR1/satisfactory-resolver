{
    "name": "satisfactory-resolver",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "sync:items": "node packages/wikisync items res/items.json",
        "sync:recipes": "node packages/wikisync recipes res/recipes.json",
        "sync:icons": "node packages/wikisync icons res/icons.json",
        "sync": "npm run sync:items && npm run sync:recipes && npm run sync:icons",
        "sync:mock": "cd res && cp items.mock.json items.json && cp recipes.mock.json recipes.json && cp icons.mock.json icons.json"
    }
}
